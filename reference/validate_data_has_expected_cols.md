# Validate that a data frame contains expected columns

Validate that all expected columns for an operation are given in a data
frame.

## Usage

``` r
validate_data_has_expected_cols(data, expected_columns)
```

## Arguments

- data:

  data frame that is to be validated

- expected_columns:

  Character vector listing the expected columns
