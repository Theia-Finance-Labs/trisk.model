# Read in AR PAMS production data.

Read in AR PAMS production data.

## Usage

``` r
read_production_data(path = NULL)
```

## Arguments

- path:

  A string that points to the location of the file containing the AR
  PAMS dataset

## See also

Other import functions: [`read_carbon_data()`](read_carbon_data.md),
[`read_financial_data()`](read_financial_data.md)
